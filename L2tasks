1:
выведет срес слайса с 2 элемента(77) по 4 элемент(79) всего 3 элемента
2:
выведет 2 а потом 1 потому что первый первая функция возвращает конкретную
переменную а не просто значение и с ним можно делать операции, что и делает функция
в дефере, функция возврощает х=1 но потом он становится х=2 вторая же функция 
возвращает просто значение типа инт а дефер никак не может повлиять на передачу данных
и в конце концов просто передается 1 с которой дефер не может сделать +1

3:
выведет нил а потом фолс 
потому что у интерфейсоф есть поле тип которое опятьже может быть а может и не быть 
если оно есть то интерфейс пустым не является пустой интрефейс это тот у которого нет 
ни типов не методов

4: 
выведет 0123456789 а потом даст ошибку тк канал еще не закрыт
а значения туда уже не поступают

5:  это опять же выведет ерр потому что проверяется непустой интерфейс

6: 
выведет 323 так как, в функцию передается указатель на массив, при 
изменении первого элемента изначальный массив тоже меняется, но потом 
у нас идет строчка с append и тк наш массив уже полный а ему нужен еще один элмент 
то в памяти создается уже новый массив ссылка на который остается в i
а старый массив теперь не связан с i и при изменении ее он сам не меняется

7:
